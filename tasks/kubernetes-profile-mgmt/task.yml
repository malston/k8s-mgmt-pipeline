---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: malston/pks-control-plane
    tag: 0.0.2

inputs:
  - name: config-repo
  - name: repo
  - name: pks-config

run:
  path: repo/tasks/kubernetes-profile-mgmt/task.sh
